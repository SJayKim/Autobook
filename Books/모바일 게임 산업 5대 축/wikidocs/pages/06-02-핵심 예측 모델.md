# 6.2 핵심 예측 모델

이 섹션은 Player Intelligence를 받쳐 주는 네 가지 핵심 예측 모델을 한 묶음으로 풀어 갑니다. LTV 예측이 UA 입찰가를 결정하는 흐름, 이탈 예측이 운영 캠페인에 연결되는 방식, 고결제 유저(whale)를 조기에 식별하는 모델, 그리고 어뷰징과 봇을 잡는 이상 탐지까지. 각 모델은 다른 모델의 결과와 운영 시스템에 입력으로 다시 들어가, Player Intelligence라는 큰 그림을 함께 만듭니다.

다음 토픽 6.2.1에서는 LTV 예측과 UA 입찰가 결정부터 봅니다.
